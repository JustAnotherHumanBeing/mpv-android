# External subtitle control

SubRip subtitle used to verify external libass composition over both the
MediaCodec-copy path and the native Dolby video Surface. The SHA-256 is in
`sha256.txt`; the subtitle payload itself is not tracked here.
